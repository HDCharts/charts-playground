# Coordinates every playground pull request check from one immutable merge revision.
name: Pull Request

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, labeled, unlabeled]

permissions:
  contents: read

concurrency:
  group: pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  prepare:
    name: Prepare PR
    runs-on: ubuntu-latest
    outputs:
      source_sha: ${{ github.sha }}
      should_run: ${{ steps.changes.outputs.should_run }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Detect code changes
        id: changes
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          should_run="$(bash scripts/ci-has-code-changes.sh "$BASE_SHA" "$HEAD_SHA")"
          echo "should_run=$should_run" >> "$GITHUB_OUTPUT"

  assemble:
    name: PR Assemble
    needs: prepare
    uses: ./.github/workflows/assemble.yml
    with:
      should-run: ${{ needs.prepare.outputs.should_run == 'true' }}
      source-sha: ${{ needs.prepare.outputs.source_sha }}

  compile:
    name: PR Compile
    needs: prepare
    uses: ./.github/workflows/compile.yml
    with:
      should-run: ${{ needs.prepare.outputs.should_run == 'true' }}
      source-sha: ${{ needs.prepare.outputs.source_sha }}

  lint:
    name: PR Lint
    needs: prepare
    uses: ./.github/workflows/lint.yml
    with:
      should-run: ${{ needs.prepare.outputs.should_run == 'true' }}
      source-sha: ${{ needs.prepare.outputs.source_sha }}

  test:
    name: PR Test
    needs: prepare
    uses: ./.github/workflows/test.yml
    with:
      should-run: ${{ needs.prepare.outputs.should_run == 'true' }}
      source-sha: ${{ needs.prepare.outputs.source_sha }}
